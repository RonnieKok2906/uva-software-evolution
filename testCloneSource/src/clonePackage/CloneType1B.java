package clonePackage;

public class CloneType1B {
	//Clone from original with more comments
	void sumProd(int n) {
		double sum = 0.0;//C1'
		double prod = 1.0;//C
		for (int i = 1; i <= n; i++)
			{sum = sum + i;
			prod = prod * i;
			foo(sum, prod); }}
}
